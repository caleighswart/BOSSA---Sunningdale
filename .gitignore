# Python
__pycache__/
*.pyc
*.pyo
.env

# Node (Netlify Functions deps — installed by Netlify / CI, never committed)
node_modules/
package-lock.json

# Mac
.DS_Store

# Sensitive
*.env
secrets.py

# Local smoke build (never deployed)
docs/_smoke.html

# Sava's source stock-count workbook — contains volumes, never commit
bar/April 26 Stock Count This one.xlsx

# Claude harness state
.claude/
bar/.claude/

# Local working files / analysis artefacts (never commit — large or personal)
Vouchers/
Bar_Stock_Agent_Design_Decision.pdf
bar/WhatsApp Audio*.opus
